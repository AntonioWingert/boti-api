# Dockerfile para desenvolvimento com hot reload
FROM node:20-alpine

# Instalar dependências do sistema necessárias para Prisma e git
RUN apk add --no-cache openssl git

# Definir diretório de trabalho
WORKDIR /app

# Copiar arquivos de dependências
COPY package*.json ./

# Limpar cache do npm e instalar dependências
RUN npm cache clean --force
RUN npm install --legacy-peer-deps

# Verificar se as dependências foram instaladas corretamente
RUN npm list @aws-sdk/s3-request-presigner @types/multer multer

# Copiar schema do Prisma
COPY prisma ./prisma

# Copiar todo o código (inclui tsconfig.json, nest-cli.json e src/)
COPY . .

# Gerar cliente Prisma
RUN npx prisma generate

# Criar diretório de sessões
RUN mkdir -p /app/sessions

# Expor porta
EXPOSE 3000

# Comando para iniciar em modo desenvolvimento com polling
CMD ["npm", "run", "start:dev", "--", "--poll=1000"]
